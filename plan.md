- [x] reload all geometry when any json input is changed
- [ ] line thickness and line colors 
- [ ] font shader to write text that is serialized, also we need to implement a text struct that is exactly the same as point just with additional attributes like text size
- [ ] create aline with arrowhead, will be a struct like line but with options if arrowhead is enabled and arrowhead size and on both ends or only on one end and which
- [ ] add a nurbs curve geometry type that is translated from opennurbs nurbs curve


